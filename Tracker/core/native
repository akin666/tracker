/*
 * native
 *
 *  Created on: 16.6.2012
 *      Author: akin
 *      
 *   Native platform functions.
 *      
 */

#ifndef COMMONNATIVE_H_
#define COMMONNATIVE_H_

#include "stdtypes"
#include "pixelbuffer"
#include "stdgfx"

#ifndef OS_NOT_DETECTED
# define USE_COMMON_NATIVELIB
#endif

namespace native 
{
	void log( const String& hint , String message );
	
	String getPath( String location );
	
	bool load( String path , String& content );
	//bool save( String path , String& content );
	
	template <class PixelType> bool load( String path , PixelBuffer<PixelType>& buffer ) { return false; }
	template <> bool load( String path , PixelBuffer<RGBALow>& buffer );
	
	template <class PixelType> bool save( String path , PixelBuffer<PixelType>& buffer ) { return false; }
	template <> bool save( String path , PixelBuffer<RGBALow>& buffer );
} // namespace native

#endif // COMMONNATIVE_H_
