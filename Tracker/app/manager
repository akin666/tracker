/*
 * manager
 *
 *  Created on: 24.10.2014
 *      Author: akin
 */

#ifndef APP_MANAGER_HPP_
#define APP_MANAGER_HPP_

#include <traccore>
#include <sampler>
#include <unordered_map>
#include "material"

class Sampler;
class Manager
{
private:
	std::unordered_map<String, Sampler::Shared> samplers;
	std::unordered_map<String, Material> materials;
public:
	Manager();
	~Manager();

	void set( String key , const Material& material );
	void set( String key , Sampler::Shared& sampler );

	bool get( String key , Material& material ) const;
	bool get( String key , Sampler::Shared& sampler ) const;

	bool hasMaterial( String key ) const;
	bool hasSampler( String key ) const;
};

#endif // APP_MANAGER_HPP_
